main 태그는 html에서 본문을 나타내는 태그이다.
=> 본문 구조상 가장 핵심을 묶기 위해 사용

header 등등의 태그와 다른 점이 한가지 있는데 그것은 바로 main 태그는 html 코드에서 
1번만 사용하는 것이다.

=> 그러므로 main은 section, nav, header,footer 안에 들어갈 수 없다.
   반대로 메인안에 저런것들이 들어간다.

왠만하면 

header, main, footer 순으로 구성하는게 좋다.

또 이 3개는 heading 태그를 사용할 필요가 없다.

header 태그의 경우 도입부에 대한 정보를 나타낸다면 
footer는 하단부에 사용한다.
