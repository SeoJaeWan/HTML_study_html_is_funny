radio : 여러가지 항목 중 1개만 선택가능 

단순 input type을 radio로만 사용하면 각각을 하나로 인식해서 모두 선택이 가능
그러므로 name 속성을 주어 서로 다른 input을 하나로 묶어줘야한다.
이 name은 서버에게 정보를 전송할 때 전송할 데이터의 명칭을 나타내기도 한다.
또 value를 통해 서버로 전송되는 정보를 지정할 수 있다.

checkbox : 여러가지 항목 중 여러 개 선택가능

기본적으로 사용하는 방법은 radio와 같다.